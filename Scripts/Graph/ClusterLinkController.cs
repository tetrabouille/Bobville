using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace BobVille.Graph
{
    public class ClusterLinkController : MonoBehaviour
    {
        [SerializeField] private ClusterController clusterA;
        [SerializeField] private ClusterController clusterB;
        [SerializeField] private List<NodeController> intersectionNodes;

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
